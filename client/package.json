{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:8080",
  "devDependencies": {
    "enzyme": "^2.6.0",
    "react-scripts": "1.0.17",
    "react-test-renderer": "^16.1.1"
  },
  "dependencies": {
    "history": "^4.7.2",
    "lodash": "^4.17.2",
    "query-string": "^5.0.1",
    "react": "^16.1.1",
    "react-dom": "^16.1.1"
  },
  "scripts": {
    "start": "export NODE_PATH=./src/ && react-scripts start",
    "build": "export NODE_PATH=./src/ && react-scripts build",
    "test": "export NODE_PATH=./src/ && react-scripts test --env=jsdom  --watchAll",
    "eject": "react-scripts eject"
  }
}
